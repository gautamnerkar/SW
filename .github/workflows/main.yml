
name: Schedule Workflow

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  Run-Script:
    runs-on: ubuntu-latest

    steps:
      - name: Print Message
        run: date
          

